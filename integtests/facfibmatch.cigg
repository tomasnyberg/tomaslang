// EXPECT 89\n89\n120
fn fib(a) {
  return match a {
    0 => 1;
    1 => 1;
    _ => fib(a-2) + fib(a-1);
  };
}

fn tfib(n) {
  return n < 2 ? 1:fib(n-2) + fib(n-1);
}

fn fac(n) {
  return match n {
    0 => 1;
    _ => n*fac(n-1);
  };
}
print(fib(10));
print(tfib(10));
print(fac(5));
